.row
  .small-12.text-center
    %h1= @campaign.name

.row
  .small-12.large-6.large-centered.columns
    %h2.text-center= t("campaigns.next_matchday")
    = render partial: "weeks/show_week", locals: { week: @next_week, show_results: false, allow_picks: true }

.row
  .small-12.large-6.large-centered.columns
    %h2.text-center= t("campaigns.last_matchday")
    = render partial: "weeks/show_week", locals: { week: @current_week, show_results: true, allow_picks: false }
