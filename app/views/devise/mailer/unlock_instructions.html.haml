%p= t("devise.mailer.unlock_instructions.hello")

%p= t("devise.mailer.unlock_instructions.has_been_locked")

%p= t("devise.mailer.unlock_instructions.click_below")

%p= link_to t("devise.mailer.unlock_instructions.unlock_link"), unlock_url(@resource, :unlock_token => @resource.unlock_token)
