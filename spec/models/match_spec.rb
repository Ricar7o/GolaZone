require 'spec_helper'

describe Match do
  # describe '#update_latest_match_in_week' do
  #   # let(:tournament) { mock_model(Tournament) }
  #   let(:week) { mock_model(Week) }
  #   let(:match) { Match.new }

  #   context "when the match is the latest one of its week so far" do
  #     it "updates the table Weeks with this match's time after the match is saved" do
  #       # match.week =
  #       match.match_time = Time.new(2013,8,5)
  #       match.save

  #       expect(week.latest_match).to eq(Time.new(2013,8,5))
  #     end
  #   end
  # end

end
